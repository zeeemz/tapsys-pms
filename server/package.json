{
  "name": "tapsys-pms-server",
  "version": "1.0.0",
  "private": true,
  "description": "Tapsys Procurement Management System — Express + Prisma backend with RBAC",
  "license": "UNLICENSED",
  "scripts": {
    "prepare:db": "node scripts/prepare-schema.js",
    "db:push": "npm run prepare:db && prisma generate && prisma db push --skip-generate --accept-data-loss",
    "predev": "npm run db:push",
    "dev": "node src/index.js",
    "prestart": "npm run prepare:db && prisma db push --skip-generate --accept-data-loss",
    "start": "node src/index.js",
    "seed": "node prisma/seed.js",
    "postinstall": "npm run prepare:db && prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.22.0"
  }
}
